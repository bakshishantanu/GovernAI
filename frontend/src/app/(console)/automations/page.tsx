import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function AutomationsPage() {
  return (
    <PagePlaceholder
      title="Automations"
      subtitle="Recipe-style rules — when this happens, do that"
      description="On the canvas but not in the backend, and not yet agreed for the capstone: whether Automations ships or stays roadmap is still an open design question. The route exists so the sidebar link does not 404."
    />
  );
}
